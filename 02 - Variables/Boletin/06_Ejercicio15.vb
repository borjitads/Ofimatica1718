Module Module1
    Sub Main()
        Dim lado As Single
        Dim areaLateral, diagonal, areaTotal, volumen As Double

        lado = 5.4

        areaTotal = 6 * Math.Pow(lado, 2)
        areaLateral = 4 * Math.Pow(lado, 2)
        diagonal = Math.Sqrt(3) * lado
        volumen = Math.Pow(lado, 3)

        'Console.WriteLine...

        Console.ReadLine()

    End Sub

End Module
